{
  "name": "eupub",
  "version": "0.1.0",
  "description": "Eupub — a standalone Windows EPUB reader that renders books in euspell reformed spelling",
  "author": "Kamran Ossia",
  "license": "GPL-3.0-or-later",
  "main": "src/main.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build:engine": "rollup -c rollup.engine.config.mjs",
    "build:engine:mobile": "rollup -c rollup.engine.mobile.config.mjs",
    "build:lexicon": "node build/copy-lexicon.js",
    "build": "npm run build:engine && npm run build:engine:mobile && npm run build:lexicon",
    "start": "npm run build && electron .",
    "dev": "electron .",
    "test": "node --test test/android-bridge.test.js && npm run build && electron test/pipeline.js && electron test/text-pipeline.js && electron test/features.js && electron test/reader-e2e.js",
    "test:smoke": "electron test/smoke.js",
    "icon": "node build/make-icon.js",
    "package": "npm run build && node build/package.js",
    "dist": "npm run build && electron-builder --win nsis",
    "dist:linux": "npm run build && electron-builder --linux AppImage"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "@rollup/plugin-node-resolve": "^15.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^26.15.3",
    "png-to-ico": "^3.0.1",
    "rollup": "^4.0.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.17"
  },
  "build": {
    "appId": "com.euspell.eupub",
    "productName": "Eupub",
    "copyright": "GPL-3.0-or-later",
    "directories": {
      "output": "release"
    },
    "files": [
      "src/**/*",
      "dist/eupub-engine.mobile.js",
      "dist/lexicon.mjs",
      "package.json",
      "!**/*.map"
    ],
    "asar": true,
    "electronVersion": "33.4.11",
    "extraResources": [
      {
        "from": "build/icon.png",
        "to": "icon.png"
      }
    ],
    "win": {
      "target": [
        "nsis"
      ],
      "icon": "build/icon.ico",
      "artifactName": "eupub-Setup-${version}.${ext}"
    },
    "linux": {
      "target": [
        "AppImage"
      ],
      "icon": "build/icon.png",
      "category": "Office",
      "mimeTypes": [
        "application/epub+zip"
      ],
      "desktop": {
        "entry": {
          "StartupWMClass": "eupub"
        }
      },
      "artifactName": "Eupub-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Eupub",
      "uninstallDisplayName": "Eupub ${version}"
    }
  }
}
